<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>SubsRetimer.Core</RootNamespace>
    <Description>Subs Re-Timer core: subtitle parsing, writing, shifting and automatic alignment (no UI)</Description>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="SubsRetimer.Tests" />
  </ItemGroup>
</Project>
